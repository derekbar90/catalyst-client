import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  mainContainer: {
    borderTopColor: 'lightgray',
    borderTopWidth: 1,
    flexDirection: 'row',
    padding: 15
  },
});